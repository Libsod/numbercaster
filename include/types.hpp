#include <cstdint>
#include <optional>
#include <string>

using i32 = int32_t;
using i64 = int64_t;
using f64 = double;
using str = std::string;

template <typename T> using Optional = std::optional<T>;
